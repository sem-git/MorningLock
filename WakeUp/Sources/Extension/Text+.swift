//
//  Text+.swift
//  WakeUp
//
//  Created by 이세민 on 1/18/26.
//

import SwiftUI

extension Text {
    func bold48(color: Color = .gray50) -> some View {
        self.font(.bold48)
            .foregroundStyle(color)
    }
    
    func bold40(color: Color = .gray50) -> some View {
        self.font(.bold40)
            .foregroundStyle(color)
    }
    
    func bold22(color: Color = .gray50) -> some View {
        self.font(.bold22)
            .foregroundStyle(color)
    }
    
    func bold20(color: Color = .gray50) -> some View {
        self.font(.bold20)
            .foregroundStyle(color)
    }
    
    func bold16(color: Color = .gray50) -> some View {
        self.font(.bold16)
            .foregroundStyle(color)
    }
    
    func heavy17(color: Color = .gray50) -> some View {
        self.font(.heavy17)
            .foregroundStyle(color)
    }
    
    func semiBold20(color: Color = .gray50) -> some View {
        self.font(.semiBold20)
            .foregroundStyle(color)
    }
    
    func semiBold17(color: Color = .gray50) -> some View {
        self.font(.semiBold17)
            .foregroundStyle(color)
    }
    
    func semiBold16(color: Color = .gray50) -> some View {
        self.font(.semiBold16)
            .foregroundStyle(color)
    }
    
    func regular20(color: Color = .gray50) -> some View {
        self.font(.regular20)
            .foregroundStyle(color)
    }
    
    func regular15(color: Color = .gray50) -> some View {
        self.font(.regular15)
            .foregroundStyle(color)
    }
    
    func regular13(color: Color = .gray50) -> some View {
        self.font(.regular13)
            .foregroundStyle(color)
    }
    
    func regular12(color: Color = .gray50) -> some View {
        self.font(.regular12)
            .foregroundStyle(color)
    }
}
